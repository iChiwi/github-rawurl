#pragma once

#include "includes.h"

// Macros
#define maxstream numeric_limits<streamsize>::max() // specifies max amount of characters in stream to ignore | use in cin.ignore
#define newline '\n'                                // newline character
